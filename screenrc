# STESH - .screenrc

startup_message off  # hide the splash screen
vbell           off  # no error bell
altscreen       on   # get rid of crap from closed programs
shell           $SHELL  # don't start login shells
autodetach      on   #
crlf            on   # detach on hangup 
defmonitor      off  # no monitoring
hardstatus alwayslastline "%{wb} $LOGNAME@%H %{kw}|%{wk} %-Lw%{= rw}%50>%{wr}%n%f* %t %{wk}%+Lw %{wk}%50> %=%{wk}%l |%C:%s %a%{-} | %Y-%m-%d%{0}"
